# Binance 涨幅榜 Supertrend 趋势策略规格

## 1. 策略目标

在 Binance USDT 永续合约 24 小时涨幅榜前 10 中，从排名最高的币开始寻找多周期向上且 5 分钟 Supertrend 刚翻多的币种，只持有一个币，并由 5 分钟 Supertrend 跟踪退出。

本策略不限制突破距离。即使 5 分钟收盘距离 Supertrend 超过 0.8 ATR，只要规则满足也必须入场，以避免主观过滤掉快速启动的趋势。

## 2. 固定参数

| 项目 | 规则 |
|---|---|
| 市场 | Binance USDT 永续合约 |
| 候选池 | 24 小时涨幅榜前 10 |
| 方向 | 只做多 |
| Supertrend | ATR 长度 10，倍数 3 |
| 方向周期 | 15m、1h、4h、1d |
| 入场与退出周期 | 5m |
| 保证金模式 | 全仓 |
| 交易所杠杆设置 | 5 倍 |
| 实际名义仓位 | 开仓时账户权益的 2 倍 |
| 最大同时持仓 | 1 个币种 |
| 加仓、补仓 | 禁止 |
| 固定止盈 | 不设置 |
| 榜单更新 | 每 60 秒 |

## 3. 数据与收盘原则

方向和信号只使用最后一根已经完成的 K 线。正在形成的 K 线可以展示，但不能改变信号。

- 15m、1h、4h、1d：最后一根已完成 K 线决定方向过滤。
- 5m：最后两根已完成 K 线决定入场穿越。
- 5m：最后一根已完成 K 线决定软退出和硬止损上移。
- 涨幅榜：使用交易所滚动 24 小时行情，每 60 秒重新排序，不等待 K 线收盘。

如果 K 线缺失、时间不连续、接口失败、时间不同步或指标无法完成预热，本轮不得产生新信号。

## 4. 高周期方向过滤

允许开多必须同时满足：

```text
Close15m > Supertrend15m
Close1h  > Supertrend1h
Close4h  > Supertrend4h
Close1d  > Supertrend1d
```

所有数值都来自同周期最后一根已完成 K 线。等于趋势线不算通过。

高周期只控制是否允许开仓。持仓以后，高周期转空不直接平仓，当前仓位继续由榜单资格、5 分钟软止损和硬止损管理。

## 5. 候选币优先级

没有持仓时，在每根 5 分钟 K 线完成后，从涨幅榜第 1 名扫描到第 10 名：

1. 必须仍在最新完整榜单前 10；
2. 15m、1h、4h、1d 全部通过方向过滤；
3. 最新完成的 5m 出现线下到线上的收盘突破；
4. 当前没有其他持仓；
5. 不处于同币种硬止损冷却期。

找到第一个满足全部条件的币后立即停止扫描。即使后续排名更低的币也满足条件，也不再选择。

持仓期间若出现排名更高的新信号，不换仓、不加仓。只有当前仓位先退出，才重新从最新榜单第 1 名开始选择。

## 6. 5 分钟入场信号

定义：

```text
C1  = 前一根已完成 5m 收盘价
ST1 = 前一根 5m 对应的 Supertrend
C2  = 最新已完成 5m 收盘价
ST2 = 最新 5m 对应的 Supertrend
ATR2 = 最新 5m 的 ATR(10)
```

入场穿越：

```text
C1 <= ST1
并且
C2 > ST2
```

完整入场条件：

```text
位于最新涨幅榜前 10
且是排名最高的合格币
且 15m、1h、4h、1d 全部向上
且 C1 <= ST1
且 C2 > ST2
且当前没有持仓
```

不检查 `(C2 - ST2) / ATR2` 的上限，不因为长阳线、涨幅过高、超买或距离趋势线过远而放弃合法信号。

## 7. 入场执行

信号在 5 分钟 K 线正式结束后确认。确认时必须重新检查：

- 币种仍在前 10；
- 当前确实没有持仓；
- 合约状态为可交易；
- 行情、账户和时间数据有效。

随后使用市价单做多：

```text
目标名义价值 = 开仓前账户权益 × 2
下单数量 = 目标名义价值 / 当前市场价格
```

数量按交易所步长向下取整，不能因取整而超过目标名义价值。交易所杠杆参数设置为 5 倍，保证金模式为全仓。

开仓后的权益变化不触发补仓，不动态维持 2 倍敞口。

## 8. 入场确认与失败处理

市价单提交后必须确认：

1. 订单成交；
2. 实际仓位为多头；
3. 持仓数量和成交均价有效；
4. 名义敞口未异常超限；
5. 初始硬止损已经成功挂出。

买入成交后若硬止损无法成功挂出，应立即使用只减仓市价单平仓，不能留下无交易所保护的全仓仓位。

## 9. 初始硬止损

严格使用入场信号 K 线的数据：

```text
HardStop0 = ST2 - ATR2
```

订单建议：

```text
类型：STOP_MARKET
方向：SELL
触发依据：MARK_PRICE
属性：只减仓
数量：全部持仓
```

硬止损用于防范断网、程序停止、接口异常和极端急跌。它不是正常趋势退出线。

## 10. 正常软止损

定义最新已完成 5m K 线：

```text
C3 = 最新已完成 5m 收盘价
ST3 = 同一根 5m 的 Supertrend
```

规则：

```text
C3 >= ST3：继续持有
C3 < ST3：全部退出
```

软退出流程：

1. 查询实际持仓；
2. 确认仍持有多头；
3. 处理当前硬止损订单；
4. 提交只减仓市价卖单；
5. 确认持仓归零；
6. 清除本地持仓状态并记录退出原因。

## 11. 硬止损动态上移

只有在 5 分钟 K 线完成且软止损未触发时计算：

```text
CandidateHardStop = 最新已完成 5m Supertrend - 最新 5m ATR
NewHardStop = max(OldHardStop, CandidateHardStop)
```

硬止损只能向上，不能向下。为了减少频繁修改，只有：

```text
CandidateHardStop - OldHardStop >= 0.5 × 当前 ATR
```

才更新交易所止损。

硬止损使用标记价格触发，以降低单笔异常成交造成误触发的概率，但无法消除真实急跌或标记价格同步下跌造成的触发。

更新保护单时必须避免出现无保护窗口；程序实现阶段需要确定采用原子修改，或先确认新保护有效再撤销旧保护。

## 12. 跌出榜单退出

每 60 秒取得一次完整有效的涨幅榜。若持仓币：

```text
排名 > 10
或不在前 10 列表中
```

立即使用只减仓市价单全部退出，不等待 5 分钟收盘。

只有完整、成功且时间有效的榜单响应才允许触发退出。接口失败、空数据、数量异常、字段缺失或过期缓存不能被解释为跌出前 10。

## 13. 三类退出及优先级

持仓可以由以下任意一种方式结束：

1. 交易所硬止损已经触发；
2. 持仓币跌出涨幅榜前 10；
3. 已完成 5m 收盘跌破 Supertrend。

处理优先级同上。执行任何本地退出前，必须查询交易所实际持仓，防止硬止损已经成交后再次卖出并意外开空。

## 14. 止盈原则

本策略不设置固定止盈、不分批退出、不因浮盈回撤主观平仓。

全部仓位持续持有，直至榜单退出、5m 软止损或硬止损首先发生。趋势上行期间，5m Supertrend 与其下方 1 ATR 的硬止损随已完成 K 线上移。

## 15. 重新入场

退出后不能因为价格仍在 5m Supertrend 上方直接买回，必须重新出现完整的：

```text
前一根已完成 5m 收在线下
最新已完成 5m 收在线上
```

并重新通过榜单与全部高周期过滤。

同一币种触发硬止损后，默认冷却 3 根完整 5m K 线；其他币种仍可参与排名扫描。该冷却规则列入待确认项。

## 16. 明确禁止

- 同时持有多个币；
- 持仓期间切换到排名更高的新信号；
- 加仓、补仓或摊低成本；
- 因主观判断涨幅过高而拒绝合法入场；
- 使用正在形成的 K 线确认方向、入场或软退出；
- 在仓位状态不明确时发送新的开仓或平仓订单；
- 因榜单接口失败而退出；
- 硬止损向下移动；
- 开仓后为了维持 2 倍敞口继续补足仓位。

## 17. 状态机

```text
WAITING_RANKING
  -> WAITING_DIRECTION
  -> WAITING_5M_CROSS
  -> ENTERING
  -> HOLDING
  -> EXITING
  -> WAITING_RANKING
```

`HOLDING`状态同时监控：

- 每 60 秒榜单资格；
- 每根已完成 5m 的软止损；
- 每根已完成 5m 的硬止损候选更新；
- 交易所实际持仓和保护单状态；
- 交易所硬止损成交事件。

## 18. 评估指标

规则确认前至少应通过历史或模拟数据记录：

- 入场排名；
- 入场时 24 小时涨幅；
- 入场收盘距离 ST 的 ATR 倍数；
- 初始硬止损距离；
- 2 倍敞口下的理论账户风险；
- 最大有利波动和最大不利波动；
- 持仓时间；
- 手续费、资金费率与滑点；
- 最终收益；
- 退出原因；
- 连续亏损次数；
- 最大账户回撤。

应重点比较入场距离 `<0.8 ATR`、`0.8～1.5 ATR`、`1.5～3 ATR`及`>3 ATR`四组结果，并分别统计软止损、跌出榜单和硬止损三类退出表现。

## 19. 已知风险

1. 取消追高限制可能在加速末端入场；
2. 权益 2 倍敞口下，远离趋势线入场会放大硬止损对应的账户损失；
3. 全仓保证金会让整个合约账户余额为仓位提供保证；
4. 滚动 24 小时排名可能在第 10、11 名之间频繁变化，造成高换手；
5. 5m Supertrend 在震荡行情中可能连续翻转；
6. 市价入场、软退出和硬止损均存在滑点；
7. MARK_PRICE 只能降低部分异常成交干扰，不能保证不被急跌触发；
8. 本地运行存在断网、休眠、程序退出和状态不同步风险；
9. 动态止损不能保证实际亏损固定；
10. 历史回测无法完整复制真实成交、滑点和资金费率。

## 20. 开发边界

当前文档仅确认策略，不授权真实下单。代码开发应分阶段进行：

1. 只读行情与指标核对；
2. 历史回测；
3. 实时信号与纸面交易；
4. Binance 测试环境；
5. 经单独确认后才考虑真实账户执行。
